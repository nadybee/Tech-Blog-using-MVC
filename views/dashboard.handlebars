<div class="blog-posts">
	<h2>Your posts</h2>
	<p>click on the blog to edit.</p>
  {{#each blogs as |blog|}}

    <div class="card mb-5 blog-id" data-blogId={{id}}>
      <div
        class="card-header bg-primary text-white d-flex justify-content-between align-items-center"
      >
     
          <h5 contenteditable="true" data-title="{{blog.blog_title}}" > {{blog.blog_title}}</h5>
 
        <div class="d-flex-grid">
          <p class="">
            Posted by
            {{blog.username}}
            on
            {{format_date blog.date}}</p>
    
        </div>
      </div>
      <div class="card-body text-dark" contenteditable="true" data-content = {{blog.contnent}}>
        {{blog.content}}
		<br>
			<div class="edit-buttons">
		<button class="btn btn-sm btn-primary m-2"  data-edit="{{id}}"> save edits</button>
	
           
            <button
              class="btn btn-link text-primary me-3 fw-lighter"
              data-delete="{{id}}"
            >delete post</button>
          </div>
		
      </div>
	     <a href="/blogpost/{{blog.id}}" class="p-3 text-dark"> read the comments </a>
    </div>
	  </div>
  {{else}}
    <p> no blog posts yet!</p>
  {{/each}}
</div>

<div
  class="container d-flex justify-content-center flex-column align-items-center"
>

  {{! <form action="/dashboard" method="POST" class ="bg-light p-3 mb-3 w-100"> }}
  <form class="bg-light p-3 mb-3 w-100">

    <h3>Create a blog post</h3>
    <div class="form-group">
      <label for="title">Title <span class="require"></span></label>
      <input type="text" class="form-control" id="blog-title" name="title" />
    </div>

    <div class="form-group mt-3">
      <label for="description">Content</label>
      <textarea
        rows="5"
        class="form-control"
        id="blog-content"
        name="description"
      ></textarea>
    </div>

    {{! <div class="form-group">
    		        <p><span class="require">*</span> - required fields</p>
    		    </div> }}

    <div class="form-group mt-3">
      <button type="submit" class="btn btn-primary" id="create-blog">
        Create
      </button>
      {{! <button class="btn btn-default">
    		            Cancel
    		        </button> }}
    </div>

  </form>
</div>


